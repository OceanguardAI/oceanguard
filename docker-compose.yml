version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./backend/data:/app/data:ro

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
